services:
  renderer:
    image: grafana/grafana-image-renderer:latest
    ports:
      - "8081:8081"

  nginx:
    image: nginx
    ports:
      - "8090:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
